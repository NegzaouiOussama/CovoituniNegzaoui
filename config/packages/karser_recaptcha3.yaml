# This file is temporarily disabled to fix the error
# karser_recaptcha3:
#    site_key: '%env(RECAPTCHA3_KEY)%'
#    secret_key: '%env(RECAPTCHA3_SECRET)%'
#    score_threshold: 0.5
#    enabled: true

karser_recaptcha3:
    site_key: 'placeholder_key'
    secret_key: 'placeholder_secret'
    score_threshold: 0.5
    enabled: true
